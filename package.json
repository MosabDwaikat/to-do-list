{
  "name": "to-do-list",
  "version": "2.0.0",
  "description": "to do list app using typescript",
  "main": "dist/index.js",
  "scripts": {
    "start": "lite-server",
    "dev": "tsc --watch",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}
